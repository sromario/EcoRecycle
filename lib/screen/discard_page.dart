import 'package:flutter/material.dart';

class DiscardPage extends StatelessWidget {
  const DiscardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dircard Page'),
      ),
    );
  }
}
